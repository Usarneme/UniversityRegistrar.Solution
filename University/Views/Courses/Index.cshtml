@using University.Models;
@* @model List<University.Models.Course>; *@

<div class="container">
  <div class="row">
    <h2 class="col-sm-8">Courses</h2>
    @Html.ActionLink("Add New Course", "Create", "Courses", null, new { @class = "btn btn-primary col-sm-3" })
  </div>
  <div class="container content pt-5">
    @if (@Model.Count == 0)

    {
      <p>There are no Courses yet registered.</p>
    }
    else
    {
      <ul>
        @foreach (Course course in Model)
        {
          <li>
            @Html.ActionLink($"{course.CourseName}", "Details", "Courses", new { id = course.CourseId }, new { @class = "btn btn-primary" })
          </li>
        }
      </ul>
    }
  </div>
</div>
